<!DOCTYPE html>
<html>
<head>
<style>
table, th, td {
    border: 1px solid blue;
    border-collapse: collapse;
}
th, td {
    padding: 5px;
}
</style>
</head>
<body>
<h3>My Todos</h3>
<table style="width:50%">
      <tr>
        <th>Todo(click on the todo link to edit/delete) </th>
        <th>Days Left</th> 
        <th>Finish_status</th>
        <th>Create At</th>
        <th>Complete</th>
        <th>Delete</th>
      </tr>
    <% @todos.each do |todo| %>
       <td><%= link_to todo.todo_item , [@user, todo] %></td>
       <td><%= todo.time_duration %></td>
       <td><%= todo.finish_status %></td>
       <td><%= todo.created_at %></td>
       <td><%= link_to "complete", user_todo_complete_path(@user, todo), method: :patch  %></td>
       <td><%= link_to "delete", [@user, todo], method: :delete  %></td>
     </tr>
    <% end %>
  </div>
</div>
</table>
<div class="row">
  <div class="col-md-4">
    <%= render partial: 'form', locals: {todo: @todo} %>
    <%#= link_to "New", new_user_todo_path(current_user, @todo), class: 'btn btn-success' %>
  </div>
</div>
</body>
</html>


 
